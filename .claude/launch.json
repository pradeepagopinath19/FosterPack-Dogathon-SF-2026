{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "fosterpack-web",
      "runtimeExecutable": "bash",
      "runtimeArgs": ["-lc", "export NVM_DIR=\"$HOME/.nvm\"; [ -s \"$NVM_DIR/nvm.sh\" ] && . \"$NVM_DIR/nvm.sh\"; nvm use --lts >/dev/null; cd web && npm run dev"],
      "port": 3000
    }
  ]
}
